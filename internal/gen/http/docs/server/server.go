// Code generated by goa v3.23.4, DO NOT EDIT.
//
// docs HTTP server
//
// Command:
// $ goa gen github.com/oleshko-g/gophermart/api/design -o internal/

package server

import (
	"context"
	"net/http"
	"path"

	docs "github.com/oleshko-g/gophermart/internal/gen/docs"
	goahttp "goa.design/goa/v3/http"
)

// Server lists the docs service endpoint HTTP handlers.
type Server struct {
	Mounts                      []*MountPoint
	InternalGenHTTPOpenapiYaml  http.Handler
	InternalGenHTTPOpenapi3Yaml http.Handler
}

// MountPoint holds information about the mounted endpoints.
type MountPoint struct {
	// Method is the name of the service method served by the mounted HTTP handler.
	Method string
	// Verb is the HTTP method used to match requests to the mounted handler.
	Verb string
	// Pattern is the HTTP request path pattern used to match requests to the
	// mounted handler.
	Pattern string
}

// New instantiates HTTP handlers for all the docs service endpoints using the
// provided encoder and decoder. The handlers are mounted on the given mux
// using the HTTP verb and path defined in the design. errhandler is called
// whenever a response fails to be encoded. formatter is used to format errors
// returned by the service methods prior to encoding. Both errhandler and
// formatter are optional and can be nil.
func New(
	e *docs.Endpoints,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(ctx context.Context, err error) goahttp.Statuser,
	fileSystemInternalGenHTTPOpenapiYaml http.FileSystem,
	fileSystemInternalGenHTTPOpenapi3Yaml http.FileSystem,
) *Server {
	if fileSystemInternalGenHTTPOpenapiYaml == nil {
		fileSystemInternalGenHTTPOpenapiYaml = http.Dir(".")
	}
	fileSystemInternalGenHTTPOpenapiYaml = appendPrefix(fileSystemInternalGenHTTPOpenapiYaml, "/internal/gen/http")
	if fileSystemInternalGenHTTPOpenapi3Yaml == nil {
		fileSystemInternalGenHTTPOpenapi3Yaml = http.Dir(".")
	}
	fileSystemInternalGenHTTPOpenapi3Yaml = appendPrefix(fileSystemInternalGenHTTPOpenapi3Yaml, "/internal/gen/http")
	return &Server{
		Mounts: []*MountPoint{
			{"Serve ./internal/gen/http/openapi.yaml", "GET", "/api/openapi.yaml"},
			{"Serve ./internal/gen/http/openapi3.yaml", "GET", "/api/openapi3.yaml"},
		},
		InternalGenHTTPOpenapiYaml:  http.FileServer(fileSystemInternalGenHTTPOpenapiYaml),
		InternalGenHTTPOpenapi3Yaml: http.FileServer(fileSystemInternalGenHTTPOpenapi3Yaml),
	}
}

// Service returns the name of the service served.
func (s *Server) Service() string { return "docs" }

// Use wraps the server handlers with the given middleware.
func (s *Server) Use(m func(http.Handler) http.Handler) {
}

// MethodNames returns the methods served.
func (s *Server) MethodNames() []string { return docs.MethodNames[:] }

// Mount configures the mux to serve the docs endpoints.
func Mount(mux goahttp.Muxer, h *Server) {
	MountInternalGenHTTPOpenapiYaml(mux, http.StripPrefix("/api", h.InternalGenHTTPOpenapiYaml))
	MountInternalGenHTTPOpenapi3Yaml(mux, http.StripPrefix("/api", h.InternalGenHTTPOpenapi3Yaml))
}

// Mount configures the mux to serve the docs endpoints.
func (s *Server) Mount(mux goahttp.Muxer) {
	Mount(mux, s)
}

// appendFS is a custom implementation of fs.FS that appends a specified prefix
// to the file paths before delegating the Open call to the underlying fs.FS.
type appendFS struct {
	prefix string
	fs     http.FileSystem
}

// Open opens the named file, appending the prefix to the file path before
// passing it to the underlying fs.FS.
func (s appendFS) Open(name string) (http.File, error) {
	switch name {
	}
	return s.fs.Open(path.Join(s.prefix, name))
}

// appendPrefix returns a new fs.FS that appends the specified prefix to file paths
// before delegating to the provided embed.FS.
func appendPrefix(fsys http.FileSystem, prefix string) http.FileSystem {
	return appendFS{prefix: prefix, fs: fsys}
}

// MountInternalGenHTTPOpenapiYaml configures the mux to serve GET request made
// to "/api/openapi.yaml".
func MountInternalGenHTTPOpenapiYaml(mux goahttp.Muxer, h http.Handler) {
	mux.Handle("GET", "/api/openapi.yaml", h.ServeHTTP)
}

// MountInternalGenHTTPOpenapi3Yaml configures the mux to serve GET request
// made to "/api/openapi3.yaml".
func MountInternalGenHTTPOpenapi3Yaml(mux goahttp.Muxer, h http.Handler) {
	mux.Handle("GET", "/api/openapi3.yaml", h.ServeHTTP)
}
